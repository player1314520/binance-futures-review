# 我们的承诺(v5)

> 版本：**v5** · 生效：2026-08-31 · 变更规则：任何改动先更新本页、测试与产品文案，再发布并保留变更记录。

## 一句话

**本地导入仍默认本地处理；邀请制 Beta 的 Binance 云同步会把专用只读 API 凭据、交易和复盘数据交给服务端处理。**

这不是“零网络”、零知识或端到端加密产品。导入 CSV 和本地分析默认不上传；用户另行连接 Binance USDⓈ-M 后，
服务端会保存加密凭据并读取交易、订单、收支、余额和仓位，交易与复盘数据均为服务端可读。项目管理员和拥有
部署环境权限的人理论上可解密 API 凭据，因此部署权限本身属于高敏感权限。

当前公开状态仍是 `local-demo`，不是邀请制 Beta 已上线。候选 Beta 最多 10 个账号，只支持 Binance USDⓈ-M，
以小时级尽力同步为运行目标，不提供实时或可用性 SLA。双用户真实账户隔离、只读权限、同步、写回复盘、断开、
删除和恢复门禁全部通过后，状态才可从 `local-demo` 提升为 `invite-beta`。

## 三类数据

| 类别 | 包含什么 | 当前处理规则 |
|---|---|---|
| **A · 内容与账户数据** | CSV、Classic 只读迁移导出、成交/订单/收支、仓位、余额、成交明细账本、复盘笔记、分析结果 | 本地 CSV 默认只在浏览器或本机 runtime 处理。邀请制 Beta 的 Binance 同步和复盘闭环会把规范化交易、复盘、行动、日志、风控和报告数据写入新服务端数据面；这些数据不是 E2EE，服务端可以读取。旧 E2EE vault 强制只读，只能由用户显式在浏览器解密后迁移，禁止双写和反向覆盖。 |
| **B · 凭据与身份数据** | Binance/LLM 凭据、生产登录邮箱与会话 | 本机 runtime 的 Binance/LLM 凭据继续由 Windows DPAPI 加密保存。邀请制 Beta 会把专用 Binance 只读 API Key/Secret 发给 Edge 服务验证，并用每连接随机 DEK + AES-256-GCM 加密保存；DEK 由不入数据库的版本化项目包装密钥再次包装。管理员和部署环境理论上可解密。 |
| **C · 网络与存在性数据** | IP、请求时间、公开行情查询、空文件计数 | 网络服务及托管商天然能看到连接元数据。当前内置匿名计数只请求同源空文件，不带账号、设备标识或交易数值。 |

## 当前网络路径

| 路径 | 何时发生 | 发送什么 | 不发送什么 |
|---|---|---|---|
| **Binance 账户同步** | 本机模式由用户在 runtime 配置；邀请制 Beta 由用户接受风险确认后连接或轮换专用只读 Key | 本机模式由 runtime 直连；Beta 将 Key/Secret 发往 Supabase Edge，校验只读权限后加密保存，并按任务读取 USDⓈ-M 的成交、收支、订单、Algo 订单、强平、余额和仓位 | 不支持交易、撤单、转账、提现或用户自定义上游 URL；响应与日志不回显凭据 |
| **Binance 公开行情** | 用户打开需要真实 K 线/基准的功能，或在独立报告页点“拉取铁证”时 | 品种、周期、起止时间窗 | 不带 CSV、成交盈亏、账号或凭据 |
| **AI** | 用户先查看字段预览，再做一次性确认时 | 仅发送确认弹层列出的聚合字段，以及用户另行勾选的逐笔、精确时间、金额或笔记 | 页面加载、切换视图和普通浏览不会自动调用 AI；未确认字段不进入 prompt |
| **邀请制 Beta 数据面** | 受邀用户登录、确认 v5 数据处理方式并连接 Binance 后 | 邮箱/会话、加密的 Binance 凭据、交易与复盘明文、覆盖/对账/任务元数据；日备份包含排除凭据后的应用数据 | 包装主密钥、Auth Secret、GitHub OIDC 临时令牌、归档短期链接不得进入浏览器、数据库备份、R2、Brevo、GitHub Runner 或日志 |
| **自定义本机/智能体端点** | 用户主动配置并连接时 | 请求路径、可选访问 token，以及该端点明确提供或接收的功能数据 | 不会因默认启动自动连接任意第三方智能体地址 |
| **匿名计数** | 独立报告生成时 | 向同源 `beacon/report-generated.gif` 请求一个空文件 | 无 cookie、无指纹、无设备 ID、无账号、无交易数值 |

### 邀请制 Beta 是服务端可读，不是零知识

新数据面为了完成服务器同步、对账、Ledger、复盘写回和报告，会处理交易及复盘明文。API Secret 入库前使用
AES-256-GCM 加密，但拥有正确包装密钥的运行环境可以解密；这不等于零知识。旧浏览器端加密 vault 不再接受新写入，
只作为显式迁移与回滚源。旧承诺下的用户必须重新确认 `consentVersion`，不得静默改变处理方式。

Supabase 没有固定出口 IP，因此本 Beta 无法启用 Binance IP 白名单。R2、Brevo 和 GitHub Runner 是外部处理方：
R2 保存排除凭据后再用离线 age 公钥加密的备份；Brevo 处理受邀邮箱和事务邮件；私有 GitHub Runner 只处理受约束
的历史归档或备份任务，不接触 Binance API Secret、项目包装密钥或 service role。

### 增量 CSV 与成交明细账本

每次通过门禁的 CSV 成交明细会并入当前工作区账本，再从全部账本重放闭环交易；指标不会直接拼接两个文件各自算出的交易结果。有稳定 Trade ID 时按“品种 + Trade ID”去重，Order ID 不作为成交唯一键。内置 Binance USD-M 适配器的精确表头标记，只是解析器对结构匹配的证据；它不是交易所签名、密码学来源证明或文件未被修改的证明。没有稳定 Trade ID 时，同品种、同持仓方向的重叠时间窗会失败关闭，避免把同一成交算两次；用户应改为导入覆盖该范围的完整文件。普通 CSV 通常没有可验证的 Binance 账户 UID，因此产品只能维持用户选择的本地账户范围，不能证明两个文件实际属于同一交易所账户。

`.fupan` 与完整便携备份会保留成交明细账本；恢复时必须校验账本哈希、成交谱系和已生成交易投影后才能继续增量导入。这些下载文件是明文，可能包含 Trade ID、Order ID、时间、价格、数量、费用和复盘内容，用户必须自行安全保存。

完整工作区替换只接受 `scope.kind = full-workspace` 的便携备份；日期范围报告或旧范围 envelope 不会被静默当成
完整备份。声明为 Demo 的备份还必须与内置确定性交易事实完全一致，恢复后继续显示为合成演示且不能写入云仓；
Binance 来源备份只能恢复为离线导入档案，不恢复 API 凭据、本机连接或实时账户身份证明。

### Classic 复盘迁移

新版只在用户点击后读取当前 origin 的 `rv-reviews` / `rv-guards`，或读取用户主动选择的 Classic 只读 JSON。
导出内容只允许复盘字段和三个旧风险数字，不包含交易、API 凭据、token 或账户标识。解析后的计划绑定当前
review scope 与完整 Trade ID 集合，默认一笔都不选；用户确认归属并逐笔选择后，只插入完全相同 Trade ID 且
当前尚无复盘的记录。旧风险数字只展示，原 Classic 键不删除。其他域名或端口的 localStorage 受同源策略隔离，
新版不能直接读取；没有完全相同 ID 的记录不会用时间、价格或盈亏近似猜测。

### 独立报告页的三类请求

独立报告页的 CSP 只允许三类请求：

1. 用户点“拉取铁证”后向 `fapi.binance.com` 查询公开 K 线，只带品种和时间窗；
2. 仅当报告由固定本机地址同源提供时，访问 `127.0.0.1:8790` 的会话与用户主动选择的本机 bundle；
3. 报告生成时向本站请求一个无载荷的匿名计数空文件。

独立报告页导入的 CSV 只在该页内存中解析，不作为这三类请求的载荷。报告页底部的网络自审面板和 CSP 可用于核对这一限定范围；它不代表完整工作台也只有这三条路径。

## 你可以自己验证

1. **离线 CSV**：页面加载完成后断网，导入 CSV 仍能完成本地解析；公开行情和外部 AI 会明确不可用或降级。
2. **AI 授权**：打开 AI 功能时先检查字段预览；取消后不应出现 AI 网络请求。
3. **构建模式**：无 Beta 环境变量的构建必须保持本地演示模式；邀请制 Beta 构建必须绑定 Vercel 权威提交、独立 Supabase 项目、唯一应用 Origin，以及独立签名的两用户现场门禁与运营回执，缺一项即拒绝构建。旧 production-vault 回执不能替代 Beta 回执。
4. **增量账本**：重复导入同一份文件不会增加成交；有 Trade ID 的后续批次可追加；没有 Trade ID 的重叠窗口必须显示拒绝而不是静默合并。
5. **本机边界**：本机工作台从 `http://127.0.0.1:8790` 建立严格会话；公网页和 `file://` 页面不能跨源读取受保护 runtime。
6. **独立报告网络面板**：页脚会列出本页实际请求；CSP 响应头限制可连接目标。
7. **源码与门禁**：隐私扫描、AI 授权回归、旧明文出口退役测试和公开暂存 allowlist 都在仓库中，可重复执行。
8. **Classic 迁移**：迁移预览必须默认全不选，已有复盘不可覆盖；切换数据集后，先前计划必须因 scope/成交集合不一致而失败。
9. **浏览器事务**：当前版本的复盘、行动、实验、日志、守则、Classic 迁移、完整恢复和用户清除必须进入同一
   跨标签页互斥域；清除提升持久化代际，清除前启动的旧任务不得在清除后复活数据。生产路径要求 Web Locks
   API；缺失时失败关闭，不允许用非原子 `localStorage` 租约冒充互斥。

## 反钓鱼铁律

**官方不会向你索要原始 CSV、API key、密码或助记词。**排错应只使用脱敏、最小化样本。任何人要求发送原始凭据或完整账户数据，都应视为高风险请求。

## 我们做不到什么(诚实边界)

1. **不能消除网络元数据**：Binance、AI 服务商、云服务商、托管商和网络运营方会看到相应连接的 IP、时间等元数据。
2. **不能保护已失陷设备**：恶意扩展、恶意软件、被控制的 Windows 会话或用户主动泄露凭据都能越过应用边界。
3. **不能替第三方作保证**：云服务商和 AI 服务商如何保存、训练、审计或响应执法请求，受其条款与配置约束。
4. **不能把客户端加密说成零元数据或绝对零知识**：身份服务和托管商仍会处理邮箱、会话、IP、时间、密文大小与访问节奏，失陷前端也能在加密前读取内容。
5. **不能替代备份**：本机数据、DPAPI sidecar 和 Ledger SQLite 都可能因磁盘损坏、清理或重装丢失；当前没有自动灾备恢复承诺。
6. **不能证明真实交易所环境已验收**：合成夹具、静态测试和 Chromium 回归不等于真实 Binance 账户、地区网络或生产部署验证。
7. **不能给出投资建议**：统计、对账和 AI 文本都可能受数据缺口、模型错误和市场变化影响，仅用于复盘。
8. **不能保证即时支持**：项目维护能力有限，响应可能以天计。
9. **不能从普通 CSV 证明账户身份、来源或自动补齐漏单**：严格 USD-M 表头仍只是结构证据；没有交易所签名、
   可验证账户 UID、完整覆盖证明和稳定成交标识时，只能按用户选择的范围复盘。重叠或缺口会拒绝或明确降级，
   不能据此宣称文件来自 Binance、属于特定账户或已与 Binance 总账对平。
10. **不能跨 origin 偷读或猜配旧复盘**：浏览器同源策略阻止新版直接读取其他域名/端口的 Classic 存储；
    没有完全相同 Trade ID 时无法证明旧记录对应当前哪笔成交，因此必须保留未匹配状态。
11. **不能约束未刷新的旧标签页**：已经加载的旧版本代码不理解新版事务与清除代际；升级后必须刷新全部旧标签页，
    再执行迁移、恢复或隐私清除。新版门禁不能证明一个仍在内存运行的旧页面已停止写入。
12. **不能提供固定出口或实时 SLA**：Supabase Edge 没有固定出口 IP，无法启用 Binance IP 白名单；Free 项目和有界任务只能把小时级同步作为尽力目标。
13. **不能把加密保存说成平台不可读**：项目管理员和拥有部署环境、包装密钥权限的人理论上可解密 Binance 凭据；交易与复盘数据本来就是服务端可读。
14. **不能证明交易完整性**：常规同步、异步归档和 CSV 都可能存在配额、历史窗口或用户漏传造成的缺口；覆盖不足时只能显示缺口并锁住强指标。
15. **不能提供交易或投资决策服务**：系统不提供下单、改单、撤单、转账、提现、收益预测或投资建议，也不支持 COIN-M、子账户或公开自助注册。
16. **不能声称删除即时覆盖所有副本**：活动数据、Auth、加密备份和外部提供商日志有不同保留周期；回执会分别写明活动数据删除时间和备份预计清理时间。

## 各方可能持有的数据

| 场景 | 可能持有的数据 |
|---|---|
| 仅使用离线 CSV/本地工作台 | 设备本地文件、成交明细账本与浏览器状态；托管商仍可能有页面访问日志 |
| 使用本机 Binance 同步 | Binance 持有账户和签名请求记录；本机保存 DPAPI 凭据密文、账户快照与诊断数据 |
| 使用邀请制 Beta Binance 同步 | Supabase 处理加密 API 凭据、交易/复盘明文、任务和覆盖元数据；Binance 持有签名请求记录；管理员和部署环境理论上可解密凭据 |
| 使用 AI | 所选 AI 服务商收到一次性确认后的 prompt；项目默认不接收该 prompt |
| 使用旧生产密文云仓 | 旧 vault 只读；身份/云服务商可持有邮箱、会话、连接元数据、密文和最小同步元数据，只有用户显式迁移时才由浏览器解密并写入新数据面 |
| 使用备份与事务邮件 | R2 可持有 age 加密且排除凭据的应用备份；Brevo 处理邀请/OTP/恢复邮件；GitHub Runner 可短暂处理受限历史归档或备份流 |
| 生成独立报告 | 托管商可能记录页面/空文件请求；CSV 内容不作为报告页网络请求载荷 |

## 变更日志

| 版本 | 日期 | 变更 | 理由 |
|---|---|---|---|
| v5 | 2026-08-31 | 增加最多 10 账号的 USDⓈ-M 邀请制 Beta；明确服务端接收并加密 Binance 凭据、交易与复盘数据服务端可读、管理员和部署环境理论上可解密；旧 vault 强制只读；披露无固定出口 IP、R2/Brevo/GitHub Runner、小时级尽力同步及独立 Beta 门禁 | 新后端为了云同步、对账、Ledger 和跨设备复盘改变了 v4 的数据处理方式，必须先重写承诺并要求用户重新确认，不能沿用 E2EE/本地优先文案造成误导 |
| v4 | 2026-08-30 | 增加 Classic 只读迁移、本地/云仓完整恢复和单变量行为实验的数据处理边界；明确迁移必须显式触发、绑定当前成交集合、默认全不选、不覆盖、不同 origin 需导出且不做近似猜配 | 防止“找回旧版数据”被误实现成静默读取、跨账户写入或不可靠交易匹配，并把新增恢复与实验能力纳入可核对承诺 |
| v3 | 2026-08-29 | 将生产 Web 明确为 CSV-first 客户端加密云仓；补充成交明细账本、增量去重、无 ID 重叠拒绝、严格表头仅为结构证据、CSV 账户身份限制、明文备份字段边界和当前 `local-demo` 投放状态；旧明文 ingest 退出生产路径 | 纠正 v2 的云仓协议口径，并防止把结果级拼接、结构匹配或用户自选范围误称为可靠对账与生产上线 |
| v2 | 2026-08-11 | 将承诺改为默认本地 + 逐路径披露；加入 Binance、AI、可选云、本机 runtime 和第三方边界；明确当前云不是端到端加密 | v1 的绝对化表述已不符合当前产品能力 |
| v1 | 2026-07-17 | 首次发布三类数据模型与报告页网络披露 | 投放前建立可验证承诺基线 |
