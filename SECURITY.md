# 安全政策

## 当前状态

脱敏后的公开候选源码和无数据入口已经发布；完整生产服务尚未通过独立后端、双用户隔离、删除、备份和限流门禁，因此还没有向个人投资者开放。`STATUS: not_distributed` 描述产品开放状态，不描述公开仓库是否存在。安全报告不会自动改变该状态或触发部署。

## 报告安全问题

- 不要在公开 issue、评论或日志中提交密钥、个人信息、真实交易记录、账户截图或可直接利用的细节。
- 仅使用与维护者已经建立的私密沟通渠道发送最小化、脱敏的复现材料。
- 如果尚无双方确认的私密渠道，请先停止传输敏感证据；当前阶段没有可公开列出的安全邮箱或漏洞接收门户。
- 报告应包含受影响版本或 commit、影响、最小复现步骤和建议的披露窗口，但不要附带真实凭据。

维护者会先确认是否可复现和影响范围，再决定修复、公告和版本计划。不存在默认漏洞赏金或响应时限承诺。

## 支持范围

安全修复优先覆盖当前维护分支和 [SUPPORT.md](SUPPORT.md) 中明确列出的目标。历史私有提交、未发布原型和未经支持的本机环境不自动获得回补；公开报告不得引用或粘贴私有历史。

## 邀请制 Beta 安全边界

邀请制 Beta 最多接入 10 个 Binance USDⓈ-M 账号。每个受邀账号对应一个彼此隔离、不可转移的专属 `OWNER` tenant；当前没有共享 tenant、`ADMIN` 或 `MEMBER` 角色。它不是零知识系统：交易与复盘关系数据由服务端读取，Binance API Key 与 Secret 也会进入服务端。每个连接使用独立随机 DEK 和 AES-256-GCM 加密，AAD 精确绑定 `tenant/connection/provider/version`；DEK 再由版本化 Supabase Project Secret 包装。主包装密钥不进入数据库、Vercel、GitHub、R2、Brevo、浏览器或日志，但拥有项目管理或部署权限的管理员理论上可解密连接凭据，因此这些权限等同于高敏感解密权限。

身份与租户边界从已验证 Supabase 会话派生。业务表启用并强制 RLS，稳定 `tenant_id` 不直接信任请求体中的 `user_id` 或 `tenant_id`；跨租户访问未知资源统一返回不可枚举的 404。数据库约束保证每个 tenant 最多一条（含非活动历史）且只能为 `OWNER` 的 membership，Edge 对 `ADMIN`/`MEMBER` 上下文失败关闭。公开注册保持关闭，旧 E2EE vault 在数据库权限层只读；旧数据迁移只能由用户明确发起，禁止新旧数据面双写。

Binance 网络出口使用编译期主机和路径 allowlist，只允许固定官方主机上的 GET/USER_DATA 查询。不存在通用代理、用户自定义 URL、跨主机重定向，也不存在 POST、PUT、PATCH 或 DELETE 交易路径。接入必须得到机器可判定的只读权限证据；任一权限字段缺失、模糊或显示交易、提现、内部转账、通用转账权限时，自动接入失败并转人工复核。Supabase Edge 没有固定出口 IP，因此本阶段无法使用 Binance IP 白名单。

三种破坏性操作互相独立：断开 Binance 会停止任务并销毁活动凭据，但保留历史复盘；删除业务数据会清除本人专属 tenant 的新旧业务数据但保留 Auth；删除账户会先停同步，再删除本人专属 tenant 的业务数据与 Auth。删除回执分别说明活动数据删除时间与备份预计清理时间，不承诺所有外部处理方副本即时消失。共享 tenant、角色委派和成员级擦除尚未实现，状态为 `NOT_READY`。

## 诚实边界

1. 本政策没有建立 24x7 值守、漏洞赏金、法定保密关系或响应 SLA。
2. 本地测试和依赖扫描不能证明生产部署、真实账户或所有第三方组件没有漏洞。
3. 项目尚无公开安全报告端点；缺少已确认私密渠道时不能安全接收敏感证明。
4. 安全修复不能防御已控制同一操作系统用户会话、浏览器或解密设备的恶意进程。
5. Free Edge、共享出口与可能暂停的免费项目只能支持小时级尽力同步，不能形成实时或可用性 SLA。
6. 自动同步、异步归档和 CSV 补证都不能证明 Binance 账户从未漏记交易；覆盖不足时指标必须锁定并显示缺口。
7. 当前个人 Beta 的整租户删除不能扩展为共享 tenant 的成员级擦除；未来若增加共享角色，必须先建立新的 lineage、授权与删除验收。
